import {colors, screenHeight, screenWidth} from '../../style'

export default styles = {
		gradient: {
			height: screenHeight,
			width: screenWidth,
			position: 'absolute'	
		},
		gradientColor: [colors.green, colors.black, colors.black, colors.black, colors.black]
}
