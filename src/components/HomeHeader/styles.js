import { normalize, normalizeHeight } from "../../utils"
import { colors } from "../../style"
export default styles = {
		dateHeader: {marginBottom: 10},
		day: {paddingRight:15},
		image: {width: normalize(32), height: normalizeHeight(30), borderRadius: 50, borderWidth: 1, borderColor: colors.grey},
	}
