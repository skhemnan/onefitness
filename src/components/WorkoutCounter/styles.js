import { normalize } from "../../utils";

export default styles = {
	counterContainer: {width: '50%', marginVertical: '8%'},
	counterText: {fontFamily: 'Inter-Bold', fontSize: normalize(25), marginTop: -7},
	counterValue: {fontSize: normalize(45)}
}